# Runtime dependencies to RUN the app + API (also what the Hugging Face Space
# installs). Dev / notebook / experiment extras live in pyproject.toml:
#   pip install -e ".[dev]"          # ruff, pytest, jupyterlab, ...
#   pip install -e ".[experiments]"  # torch (Mult-VAE, notebook 07 / src/deep.py)

# Core data stack
pandas>=3.0.5
numpy>=2.4.6
scipy>=1.17.1
pyarrow>=25.0.1

# Modeling (EASE is numpy/scipy; ALS for the MMR diversity control needs implicit)
implicit>=0.7.3
threadpoolctl>=3.6.0

# App (Streamlit front-end)
streamlit>=1.62.0
plotly>=7.0.0

# API (FastAPI service)
fastapi>=0.141.1
uvicorn[standard]>=0.52.4
pydantic>=2.13.4

# Config
pyyaml>=6.0.3
python-dotenv>=1.2.3
