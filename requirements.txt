# Convenience pin file. `pyproject.toml` is the canonical dependency source
# (pip install -e ".[dev]"); this mirrors it for `pip install -r requirements.txt`.
#
# Core data stack
pandas>=2.1
numpy>=1.26
scipy>=1.13        # sparse interaction matrix
pyarrow>=14.0      # parquet

# Modeling
implicit>=0.7.2    # ALS / BPR / item-item for implicit feedback
threadpoolctl>=3.2 # pin BLAS threads around ALS fit (implicit ships it; we import it)
# NOTE: local interpreter is Python 3.14. As of this writing `implicit` may not
# publish cp314 wheels. If `pip install implicit` fails, use a 3.11/3.12 venv for
# the modeling milestones (EDA does not need implicit). See AGENTS.md.

# Serving
fastapi>=0.110
uvicorn[standard]>=0.29
pydantic>=2.6

# App (Streamlit front-end — also what the Hugging Face Space installs)
streamlit>=1.40
plotly>=5.20

# Config
pyyaml>=6.0
python-dotenv>=1.0

# Notebooks
jupyterlab>=4.0
ipywidgets>=8.1

# Visualization
matplotlib>=3.8
seaborn>=0.13

# Testing
pytest>=8.0
httpx>=0.27        # FastAPI TestClient (tests/test_api.py)

# Optional: the deep-learning experiment (Mult-VAE, notebook 07 / src/deep.py).
# Heavy; install only to reproduce that notebook:  pip install "torch>=2.2"
# torch>=2.2
